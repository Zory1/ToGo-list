var app = window.app || {};
app.Map = function () {
    'use strict';
    //not sure I will need map class - will decide later. Probably wont, unless there are multiple maps types and I get as far as caching some data for them, to minimize number of api calls.
};